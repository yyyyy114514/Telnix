fastapi>=0.111
uvicorn>=0.30
httpx>=0.27
psutil>=5.9
cryptography>=42.0
pyinstaller>=6.0
brotli>=1.1
h2>=4.1.0
mcp>=1.2
mitmproxy>=10.0
# 可选依赖（代码已优雅降级，未安装不影响核心功能）：
# py-ip2region>=3.0.0    # IP 属地查询（pip install py-ip2region）
# pydivert>=0.2.4        # Windows TCP/UDP 抓包（仅 Windows，pip install pydivert）
