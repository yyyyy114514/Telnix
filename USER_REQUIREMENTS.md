# 用户需求清单（全部完成后删除本文件）

> 记录时间：2026-08-04

## 抓包 / 详细信息展示
- [ ] **req1** 抓包页 ws 包不要分 send 和 recv，只放一个；详细信息里上面 send、下面 recv。ws 页面（专门页）仍保留分 send 和 recv。
- [ ] **req2** ws 页的包的 raw 选项卡有问题，和抓包页的详细信息的 raw 不同 —— 需统一。
- [ ] **req12** tcp/udp 抓包详细信息不要「原始信息」那一栏，只能选请求和响应。

## Hex 查看器
- [ ] **req3** 所有 hex 都不要给搜索按钮，同时禁用 ctrl+f，总之 hex 不能调出搜索框。

## 性能 / 断点
- [ ] **req4** 断点出包很卡（1 秒只出 2 个）；断点时包上的高亮延迟 1 秒多才渲染；查看详细信息也要加载约 1 秒，否则全是空白。需修复。
- [ ] **req5** 抓包、断点的开关都有延迟，还可能在开关之间闪烁 1-2 下，修复。
- [ ] **req14** 刚启动时从设置文件加载大概有 3 秒的加载时长，期间设置都不生效，修复。

## 时序图
- [ ] **req6** 时序图返回箭头渲染有问题。
- [ ] **req7** 时序图可以在「设置 - 检查器」标签页那里选择是否开启，默认关闭。

## 触发式捕获
- [ ] **req8** 触发式捕获的弹窗太不实用，改成像「筛选按钮」的弹窗那样可以配置各个条件。

## 布局 / 分割线
- [ ] **req9** 抓包页详细信息的分割线拉动可能挡住自动滚动等按钮，增加限制，保证无法遮挡，默认也不遮挡。
- [ ] **req10** ws、tcp/udp 的抓包详细信息分隔栏可像抓包页一样左右调节；同样保证无法遮挡菜单按钮，默认也不遮挡。
- [ ] **req13a** tcp/udp 在进程/端口等太长时，十六进制/解析并排按钮会换行，进程/端口显示也会换行，需解决。

## 功能修复
- [ ] **req11** 忽略现在对 tcp/udp 不生效，修复。

## 收尾
- [ ] **req13b** 做完后自己构建前端测试。
- [ ] **req15** 全部修完后调查 telnix 还缺哪些其他抓包软件具备且高价值的功能（先不做），给出难度与价值报告。
- [ ] **req16** 把用户要求记录在文件中，全部完成再删除本文件。

## 备注
- req17：不在沙箱里，可以直接执行命令构建测试。
