# 用户需求清单（全部完成后删除此文件）

> 记录时间：2026-08-04

## 需求列表

- [ ] **1. 抓包页 WS 包合并展示**：抓包页的 ws 包不要分 send 和 recv，只放一条，详细信息里上面 send 下面 recv；ws 专页里再分 send 和 recv。
- [ ] **2. WS 页 raw 选项卡修复**：ws 页的包的 raw 选项卡有问题，和抓包页详细信息的 raw 不同，需要统一。
- [ ] **3. hex 禁用搜索**：所有 hex 都不要给搜索按钮，同时禁用 Ctrl+F，总之 hex 不能调出搜索框。
- [ ] **4. 断点性能优化**：断点出包很卡（1 秒只出 2 个），断点时包上高亮延迟 1 秒多才渲染，查看详细信息也要加载约 1 秒否则全是空白。
- [ ] **5. 开关延迟/闪烁修复**：抓包、断点开关有延迟，还可能在开关之间闪烁 1-2 下。
- [ ] **6. 时序图返回箭头渲染修复**。
- [ ] **7. 时序图开关设置项**：设置的检查器标签页可选择是否开启时序图，默认关闭。
- [ ] **8. 触发式捕获弹窗改造**：改成像筛选按钮的弹窗那样，可配置各个条件。
- [ ] **9. 抓包页详情分割线限制**：分割线拉动可能挡住自动滚动等按钮，增加限制保证无法遮挡，默认也不遮挡。
- [ ] **10. ws/tcp/udp 详情分隔栏可左右调节**：像抓包页一样，且保证无法遮挡菜单按钮，默认也不遮挡。
- [ ] **11. 忽略功能对 tcp/udp 生效**：现在对 tcp/udp 不生效，修复。
- [ ] **12. tcp/udp 详情去掉原始信息栏**：只保留请求和响应。
- [ ] **13. tcp/udp 进程/端口过长换行修复**：进程/端口太长时十六进制/解析并排按钮会换行，进程/端口显示也会换行，需要解决。
- [ ] **14. 启动设置加载延迟修复**：刚启动时从设置文件加载约 3 秒，期间设置都不生效。
- [ ] **15. 调查报告（先不做）**：调查 telnix 还有哪些其他抓包软件具备且高价值但未实现的功能，给难度和价值的报告。

## 备注
- 完成后自行构建前端测试。
- 全部完成后删除本文件。
